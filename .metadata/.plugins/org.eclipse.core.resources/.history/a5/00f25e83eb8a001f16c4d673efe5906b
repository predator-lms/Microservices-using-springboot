spring :
  application :
  name : config-server-service

  cloud :
    config :
      server :
        git :
          uri : https://github.com/predator-lms/environment-variable-repo.git

server :
  port : ${port:8888}
